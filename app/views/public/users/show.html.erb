<% if @user == Current.user %>
  <h2>マイページ</h2>
<% else %>
  <h2><%= @user.name %> さんのプロフィール</h2>
<% end %>

<% content_for :custom_flash do %>
  <div class="container mt-3">
    <%= render "layouts/flash" %>
  </div>
<% end %>

<div style="border: 1px solid #ccc; padding: 15px; margin-bottom: 20px;">
  <h3>ユーザー情報</h3>
  <div style="margin-bottom: 15px;">
    <% if @user.profile_image.attached? %>
      <%= image_tag @user.profile_image, style: "width: 100px; height: 100px; object-fit: cover; border-radius: 50%;" %>
    <% else %>
      <%= image_tag "no_image.jpg", style: "width: 100px; height: 100px; object-fit: cover; border-radius: 50%;" %>
    <% end %>
  </div>
  <p><strong>名前:</strong> <%= @user.name %></p>

  
  <% if @user == Current.user %>
    <p><strong>メールアドレス:</strong> <%= @user.email_address %></p>
    <p>💬 投稿したコメント数: <%= @user.comments.count %>件</p>

    <div>
      <%= link_to "プロフィールを編集する", edit_user_information_path %>
    </div>
  <% end %>
</div>

<hr>

<h3><%= @user == Current.user ? "自分の投稿一覧" : "#{@user.name} さんの投稿一覧" %></h3>
<% if @posts.any? %>
  <% @posts.each do |post| %>
    <div style="border: 1px solid #eee; padding: 10px; margin-bottom: 10px;">
      <h4><%= link_to post.title, post_path(post) %></h4>
      <p><strong>場所:</strong> <%= post.location_name %></p>
      <p><%= truncate(post.body, length: 100) %></p>
      <small>投稿日時: <%= post.created_at.strftime("%Y/%m/%d %H:%M") %></small>
    </div>
  <% end %>
<% else %>
  <p>まだ投稿がありません。</p>
<% end %>


<% if @user == Current.user %>
  <hr style="margin-top: 30px;">

  <div style="margin-top: 20px;">
    <%= link_to "退会手続きへ進む", unsubscribe_user_information_path, style: "color: red;" %>
  </div>
<% end %>